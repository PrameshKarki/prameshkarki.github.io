class Language {
    color: string;
    name: string;
    percent: number;

    constructor(color: string, name: string, percent: number) {
        this.color = color;
        this.name = name;
        this.percent = percent;
    }
}

export default Language;